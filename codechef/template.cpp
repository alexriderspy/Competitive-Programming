#include<bits/stdc++.h>

using namespace std;

#define ll long long
#define ld long double
#define MOD 10000007
#define gen(i,a,b) for(ll i=a;i<b;i++)

void solve()
{   
    ll n;
    cin>>n;
    vector<ll>seq;
    seq.reserve(n);
    for(ll i=0;i<n;i++)
    {
        cin>>seq[i];
    }

}
int main()
{
    ll t;
    cin>>t;
    while(t--)
    {
        solve();
    }
}